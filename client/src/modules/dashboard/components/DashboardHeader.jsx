import React from 'react';

export const DashboardHeader = ({state}) => {
    return (
        <header className="dashboard-header">
            <h2 className="dashboard-title">Dashboard</h2>
        </header>
    );
};